doctrine:
    dbal:
        driver: 'pdo_sqlite'
        server_version: '11.4'
        charset: utf8
        default_table_options:
            charset: utf8
            collate: utf8_unicode_ci

        url: 'sqlite:///:memory:'
